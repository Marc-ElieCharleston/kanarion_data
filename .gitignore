# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Temp
*.tmp
*.bak
*.log

# Node (si utilisé pour validation)
node_modules/

# Claude Code local config
.claude/

# Local state for the Discord patch-notes publisher (which versions have
# been posted). Keep out of git so two machines posting the same patch
# don't accidentally conflict.
scripts/.posted_versions

# Local env vars (DISCORD_PATCH_WEBHOOK_URL, etc.) — NEVER commit webhook
# URLs or secrets. Auto-loaded by scripts/post_patch_to_discord.py.
.env

# Godot auto-generated import files (pollution quand le repo est dans un projet Godot)
*.import

# Lore documents (private, managed separately in kanarion_lore/)
Kanarian Online Lore*
world/lore.json
